// Created so that the struct type can be used throughout the files in the folder
#ifndef Coord_H
#define Coord_H

typedef struct
{
    double x, y;
} Coord; // Struct data type that will hold the coordinates of a point

#endif